{
    "extends": ["//"],
    "tasks": {
        "provision": {
            "cache": false,
            "env": [
                "POSTGRES_HOST",
                "POSTGRES_PORT",
                "POSTGRES_DB",
                "POSTGRES_USER",
                "POSTGRES_PASSWORD"
            ]
        }
    }
}
