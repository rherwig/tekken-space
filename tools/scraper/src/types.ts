export enum WavuTables {
    Move = 'Move',
}

export enum WavuFields {
    Tables = 'tables',
    Fields = 'fields',
    Where = 'where',
    JoinOn = 'join_on',
    GroupBy = 'group_by',
    Having = 'having',
    OrderBy = 'order_by[0]',
    OrderByOptions = 'order_by_options[0]',
    Limit = 'limit',
    Offset = 'offset',
    Format = 'format',
}
